# Secure Vault — طراحی کامل (opencode-ready)

> این سند بر اساس بحث طراحی با کاربر (مکس) نوشته شده. برای پیاده‌سازی با opencode
> به‌عنوان task description استفاده شود. تمام تصمیم‌ها نهایی‌اند مگر اینکه صریحاً
> «باز بماند» نوشته شده باشد.

## ۰. خلاصه‌ی یک‌خطی

یک **Vault شخصیِ رمزنگاری‌شده** با رابط گرافیکی، که تمام دسترسی‌های ایجنت (از طریق
MCP) و کارهای روزمره‌ی کاربر (از طریق API محلی + UI) را مدیریت می‌کند؛ محرمانه‌ترین
فایل‌ها از دید ایجنت پنهان یا فقط «قابل‌درخواستِ نمایش» هستند، و همه‌چیز قابل ردیابی
(لاگ کامل). جایگزین نهایی Joplin است.

---

## ۱. تصمیم‌های فنی نهایی

| موضوع | تصمیم | دلیل |
|---|---|---|
| زبان | **Python** (3.11+) | پوشش کامل QtWebEngine/tray، اکوسیستم غنی، تجربه‌ی قبلی کاربر در czasu-tracker |
| GUI | **Qt (PySide6)** | WebEngine برای markdown، QSystemTrayIcon برای tray |
| ایندکس فول‌تکست | **اسکی‌لایت FTS5** (بسته‌ی `sqlite3` + توابع FTS5) | بدون دیتابیس جدا، ساده، جاسازی‌شده |
| دیتابیس متادیتا/لاگ | **اسکی‌لایت** | همان فایل‌ای که FTS5 داخلش است |
| جستجوی معنایی | **محلی**، فقط وقتی فعال شود | هیچ ارسال محتوا به بیرون. امبدینگ محلی |
| رمزنگاری | فایل کامل به‌صورت یک blob (AES-256-GCM) | ساده، سینک/نگه‌داری راحت |
| فایل‌های بالای ۱۰MB | **اصلاً رمز نمی‌شوند** (پلین رها می‌شوند) | تصمیم کاربر |
| لایسنس | **GPL-3.0** | |
| Portable | پوشه‌ی standalone با Python embedded + venv فریز | بدون نیاز به نصب |
| پلتفرم | Windows و Kubuntu (دو بیلد جدا) | |
| رابط ایجنت | **MCP server** + **API محلی** برای UI | هر دو |

---

## ۲. معماری کلی

```
                    ┌─────────────────────────────────────┐
                    │          Secure Vault (Python)       │
                    │                                       │
  User (Qt UI) ───► │  ┌─────────────┐   ┌───────────────┐  │
                    │  │  Core Layer │   │  Vault FS     │  │
  Agent (MCP) ───►  │  │  (کریپتو،   │   │  (رمز/پلین    │  │
                    │  │  ایندکس،    │   │   فایل‌ها،     │  │
                    │  │  لاگ، حمل)  │   │   یادداشت‌ها)  │  │
                    │  └─────┬───────┘   └───────┬───────┘  │
                    │        └────────┬──────────┘         │
                    │                 ▼                    │
                    │        ┌───────────────┐             │
                    │        │  SQLite       │             │
                    │        │  (FTS5 +      │             │
                    │        │   metadata +  │             │
                    │        │   logs)       │             │
                    │        └───────────────┘             │
                    └─────────────────────────────────────┘
          سینک به Dropbox/OneDrive/Google Drive (همه‌چیز ciphertext)
```

سه لایه‌ی اصلی:
1. **Vault FS** — جایی که فایل‌ها به‌صورت فیزیکی ذخیره می‌شوند (روی دیسک، در یک پوشه‌ی
   سینک‌شونده). فایل‌های رمز شده با پسوند/فرمت خاص، فایل‌های بالای ۱۰MB پلین.
2. **Core Layer** — منطق کسب‌وکار: رمزگشایی/رمزنگاری، ایندکس‌گذاری، جستجو، عملیات
   فایل، مدیریت سطوح حساسیت، لاگ‌گیری، یادداشت فولدر.
3. **رابط‌ها** — MCP server (برای ایجنت) و API محلی (برای Qt UI). هر دو از Core
   استفاده می‌کنند؛ هیچ منطقی جدا در UI نداریم.

---

## ۳. رمزنگاری (مهم‌ترین بخش)

### ۳.۱. اصل پایه
- **Master password** توسط کاربر وارد می‌شود (هر بار اجرا، یا کش‌شده).
- از آن **Master Key** مشتق می‌شود (Argon2id یا PBKDF2-SHA256 — پیشنهاد Argon2id).
- برای هر فایل، یک **کلید per-file** از Master Key با **HKDF-SHA256** مشتق می‌شود.
- هر فایل (متنی یا تصویری) **کامل به‌صورت یک blob** با AES-256-GCM رمز می‌شود.
- **Nonce/IV** به‌همراه ciphertext ذخیره می‌شود (با فرمت ثابت).

### ۳.۲. ذخیره‌سازی روی دیسک
- فایل‌های رمز شده: `<name>.enc` (یا پسوند دلخواه) حاوی:
  ```
  [magic: 4-byte] [version: 1-byte] [salt: 16-byte] [nonce: 12-byte] [ciphertext]
  ```
  بهتر است salt یک‌بار برای کل Vault در فایل‌ی جدا (مثلاً `.vault.meta`) نگه‌داری شود تا
  حجم هر فایل کم بماند؛ یا salt per-file. تصمیم: **salt + nonce در همان فایل** (ساده‌تر،
  خودشامل، برای سینک راحت‌تر).
- فایل‌های بالای ۱۰MB: **پلین** ذخیره می‌شوند، بدون هیچ رمزی.

### ۳.۳. گریدهای امنیتی در رمزنگاری
- کلید اصلی هیچ‌وقت روی دیسک ذخیره نمی‌شود؛ فقط حین اجرا در حافظه.
- در هنگام غیرفعال بودن (locked)، کلید از حافظه پاک می‌شود.
- **نکته‌ی مربوط به semantic search:** امبدینگ‌ها نیمه‌محتوا هستند؛ اگر ایندکس معنایی
  درست شود، باید یا خودش رمز شود، یا واضح باشد که فقط حین unlock محفوظ است.

---

## ۴. سطوح حساسیت فایل‌ها

سه سطح داریم. این جدول مرجع دسترسی است:

| سطح | رمز شدن | MCP: اسم قابل دیدن؟ | MCP: محتوا؟ | MCP: درخواست «باز کن»؟ | جستجو در محتوا (MCP)؟ | باز شدن در UI |
|---|---|---|---|---|---|---|
| **عادی** | ✅ | ✅ | ✅ | ✅ | ✅ (لفظی/معنایی) | مرورگر (وب، markdown) |
| **محرمانه** | ✅ | ✅ + برچسب «محرمانه» | ❌ | ❌ | ❌ | مرورگر (با تأیید UI + notification tray) |
| **رمز** | ✅ | ✅ + برچسب «رمز» | ❌ | ✅ (فقط درخواست نمایش) | ❌ | نمایشگر OS متنی ساده |

### ۴.۱. توضیح‌ها
- **پس‌وند/Flag:** سطح حساسیت به‌صورت متادیتا در اسکی‌لایت یا یک فایل metadata
  ذخیره می‌شود، نه در نام فایل.
- **علامت‌گذاری:** علامت‌گذاری (عادی→محرمانه/رمز) از MCP ممکن است. **برخی از حالت
  محرمانه/رمز فقط از UI انجام می‌شود** (قفل یک‌طرفه).
- **فایل رمز در UI:** «نمایشگر مخصوص همان OS» — یک پنجره‌ی متنی ساده (native dialog)
  بدون markdown rendering، قابل انتخاب/کپی، نوشتنی. (نه مرورگر، چون محتوا نباید به
  موتور وب برود.)

---

## ۵. ذخیره‌سازی و اسکی‌لایت

### ۵.۱. طرح جداول (پیشنهادی)
```sql
-- متادیتای فایل‌ها (اسم واقعی توی VaultFS)
CREATE TABLE files (
    id INTEGER PRIMARY KEY,
    logical_path TEXT NOT NULL,      -- مسیر منطقی داخل Vault (مثلاً notes/foo.md)
    vault_path TEXT NOT NULL,        -- مسیر واقعی روی دیسک (ciphertext/plain)
    size INTEGER,
    encrypted INTEGER NOT NULL,      -- 1 = رمز شده، 0 = پلین (>10MB)
    sensitivity TEXT NOT NULL DEFAULT 'normal',  -- normal | secret | secretfile
    is_dir INTEGER DEFAULT 0,
    mtime INTEGER
);

-- یادداشت فولدر (قابل نوشتن/خواندن از MCP)
CREATE TABLE folder_notes (
    id INTEGER PRIMARY KEY,
    folder_path TEXT NOT NULL,
    note_text TEXT,
    updated_at INTEGER
);

-- لاگ کامل دسترسی‌های MCP (نمایش در UI)
CREATE TABLE access_log (
    id INTEGER PRIMARY KEY,
    ts INTEGER NOT NULL,
    source TEXT NOT NULL,           -- 'mcp' | 'ui'
    tool TEXT NOT NULL,             -- نام ابزار
    target_path TEXT,
    details TEXT,
    outcome TEXT
);

-- ایندکس FTS5 برای محتوای متنی (فقط فایل‌های عادی)
CREATE VIRTUAL TABLE fts_content USING fts5(
    content, file_id UNINDEXED, tokenize='unicode61'
);
```
> `sensitivity` دو حالت جدا برای «محرمانه» و «رمز». جدول بالا سه تا را با یک ستون
> `sensitivity` پوشش می‌دهد. (اختیاری: «فوق‌محرمانه») — اگر بخواهیم، ستون جدا.

### ۵.۲. ایندکس
- **فول‌تکست:** فقط فایل‌های عادی ایندکس می‌شوند (نه محرمانه/رمز، چون محتوا نباید
  در حالت قفل دیده شود؛ و نه فایل‌های باینری).
- **اسم فایل:** همه‌ی فایل‌ها (از جمله رمز) در `files.logical_path` هستند و قابل جستجو
  در اسم.

---

## ۶. جستجو — سه نوع مجزا (بدون هیبرید)

سه جستجوی جدا و مستقل، **هیچ ترکیبی وجود ندارد**:

1. **جستجو در نام فایل‌ها** — روی `files.logical_path`؛ همه‌ی فایل‌ها (عادی+محرمانه+رمز)
   در این جستجو دیده می‌شوند، چرا که فقط اسم است.
2. **جستجوی لفظی در محتوای متنی** — FTS5 روی `fts_content`؛ فقط فایل‌های عادی.
3. **جستجوی معنایی در محتوای متنی** — امبدینگ محلی؛ فقط وقتی فعال شود؛ فقط فایل‌های
   عادی.

### ۶.۱. جستجوی معنایی (اختیاری و محلی)
- وقتی فعال شد، از یک مدل embedding محلی استفاده می‌شود (مثلاً `sentence-transformers`
  با مدل کوچک مثل `all-MiniLM-L6-v2`).
- **هیچ ارسال محتوا به بیرون نیست.**
- ایندکس معنایی فقط پس از unlock (بعد از واردکردن master password) ساخته/استفاده
  می‌شود و فقط در حافظه/یا با رمز محافظت می‌شود.
- این قابلیت **به‌صورت opt-in** است و وابستگی‌های سنگینش (مدل) جدا از بیلد پایه.

---

## ۷. رابط MCP (برای ایجنت)

### ۷.۱. Resources (resource-oriented)
URI های معنی‌دار، مثل Joplin MCP:
- `vault://list` — فهرست فولدرها/فایل‌ها
- `vault://folder/<path>` — محتویات یک فولدر + یادداشتش
- `vault://note/<path>` — محتوای یک فایل عادی
- `vault://search?q=...` — نتیجه‌ی جستجو

### ۷.۲. Tools
- `list_folder(path)` — فهرست (اسم‌ها + سطح حساسیت + آیا رمز است)
- `read_file(path)` — محتوای فایل عادی (به‌صورت کامل یا سطر‌های مشخص)
- `read_lines(path, start, count)` — دسترسی به یک یا چند خط
- `write_lines(path, mode, text, at_line?)` — append یا insert در یک فایل متن
- `search_filenames(query)` — جستجو در اسم فایل‌ها
- `search_text(query)` — جستجوی لفظی در محتوای فایل‌های عادی
- `search_semantic(query)` — جستجوی معنایی (فقط اگر فعال شده باشد)
- `file_ops` — ساخت/کپی/حرکت/حذف فایل یا فولدر
- `set_sensitivity(path, level)` — علامت‌گذاری (عادی/محرمانه/رمز) — از MCP **فقط
  علامت‌گذاری به بالا** مجاز است (عادی→محرمانه→رمز)؛ برگرداندن فقط از UI.
- `request_open_secret(path)` — برای فایل‌های رمز: درخواست نمایش به کاربر → پنجره‌ی
  native باز می‌شود که **خود کاربر می‌بیند و کپی می‌کند**؛ ایجنت محتوا را نمی‌گیرد.
- `read_folder_note(path)` / `write_folder_note(path, text)` — یادداشت فولدر
- `get_access_log(...)` — مشاهده‌ی لاگ (برای ایجنت هم مجاز؟ معمولاً فقط UI)

### ۷.۳. قواعد امنیتی MCP
- فایل‌های محرمانه یا رمز: محتوا هرگز به MCP برنمی‌گردد.
- برای محرمانه: دسترسی به محتوا → فقط وقتی UI تأیید کند + notification tray.
- برای رمز: فقط `request_open_secret` → نمایش native به کاربر (نه بازگشت محتوا به ایجنت).

---

## ۸. رابط API محلی (برای Qt UI)

همان Core را سرو می‌کند ولی بدون محدودیت‌های امنیتی MCP (کاربر خودش است).
- مدیریت master password / unlock / lock
- لیست فایل‌ها، بازکردن/ویرایش (ادیتور markdown در QtWebEngine)
- عملیات فایل، علامت‌گذاری سطوح (شامل برگرداندن از رمز/محرمانه)
- دیدن لاگ
- تنظیمات (فعال‌سازی جستجوی معنایی، انتخاب پوشه‌ی سینک، ...)

---

## ۹. نمایشگر OS برای فایل‌های رمز

- یک **پنجره‌ی native ساده** (مثلاً `QDialog` با `QPlainTextEdit` فقط‌خواندنی/نوشتنی)
  که محتوای فایل رمز را **به‌صورت متن ساده** (نه markdown) نشان می‌دهد.
- کاربر می‌تواند کل یا بخشی را انتخاب و کپی کند.
- به‌طور خاص: پنجره به webview/browser نمی‌رود (جلوگیری از نشت محتوا به موتور وب).

---

## ۱۰. Tray و Notification

- **Tray:** `QSystemTrayIcon` در Windows و Kubuntu.
- وقتی **یک فایل محرمانه** از طریق MCP مورد دسترسی قرار می‌گیرد (مثلاً درخواست
  محتوا/بازکردن)، یک **notification** به کاربر نمایش داده می‌شود:
  «فایل محرمانه "X" به‌تازگی از طریق MCP درخواست شد».
- برای فایل‌های رمز هم می‌توان notification داد (به‌ویژه هنگام `request_open_secret`).

---

## ۱۱. لاگ کامل دسترسی‌های MCP

- هر فراخوانی MCP (tool/resource) در `access_log` ثبت می‌شود: زمان، ابزار، مسیر هدف،
  جزئیات، نتیجه.
- **نمایش در UI:** یک پنل لاگ در برنامه که همه‌ی این‌ها را می‌بیند.
- لاگ فقط‌الحاقی است؛ پاک نمی‌شود (به‌جز به‌درخواست کاربر صریح).

---

## ۱۲. مدیریت Master Password / Unlock-Lock

- برنامه با master password باز می‌شود (unlock).
- پس از unlock، کلیدها در حافظه هستند؛ جستجوی معنایی/فول‌تکست روی داده‌ی رمزگشایی‌شده
  کار می‌کند.
- Lock (قفل‌کردن مجدد): کلیدها از حافظه پاک، MCP فقط متادیتا/اسم/سطح می‌بیند (نه محتوا).
- **در حالت locked:** حتی فایل‌های عادی هم قابل‌خواندن از MCP نیستند (چون رمز شده‌اند و
  کلید نیست). فقط متادیتا/اسم/لیست/ یادداشت فولدر (اگر metadata جدا و بدون رمز باشد).

---

## ۱۳. سینک (Dropbox/OneDrive/Google Drive)

- تمام فایل‌های رمز شده + فایل‌های پلین (>10MB) در یک **پوشه‌ی سینک‌شونده** قرار دارند.
- چون همه‌چیز ciphertext (یا پلینِ عمدی) است، سینک امن است.
- ایندکس/لاگ/متادیتا «در مکان متفاوتی» ذخیره می‌شوند (می‌تواند داخل همان پوشه، ولی
  جدا از Vault FS، یا در پوشه‌ی جدا). تصمیم: **در پوشه‌ی سینک جدا با نام `.vault-index/`**
  ولی این هم ممکن است رمز لازم باشد (به‌ویژه ایندکس معنایی).
- سینک به‌صورت خودکار از طریق ابر انجام می‌شود؛ خود برنامه سینک نمی‌کند (فقط فایل‌ها
  را در پوشه‌ی درست می‌گذارد).

---

## ۱۴. ایمپورت از اپ‌های دیگر

- **Importer** جدا و یک‌بارمصرف برای هر اپ نوشته می‌شود.
- فعلاً: Joplin (تبدیل به vault)، داستان‌نویسی (stories)، و بعدها هر چه لازم شود.
- هر importer: داده‌ی خروجی اپ را می‌خواند، به Vault FS (رمز یا پلین) منتقل می‌کند، و
  متادیتا/ایندکس را به‌روز می‌کند.
- **Joplin:** از ساختار JEX/export یا از آینه استفاده کند. (محتوای آن markdown است.)
- Keepass: **تصمیم کاربر = اصلاً پشتیبانی نمی‌شود.** پسوردها به‌صورت فایل متن/محتوای
  عادی در Vault قرار می‌گیرند (اگر محرمانه باشند، به‌صورت «رمز» بایگانی می‌شوند).

---

## ۱۵. Portable بودن

- **Windows:** پوشه‌ی `vault-win/` شامل `python.exe` (Python embedded/venv فریز) + برنامه + Qt
  + کد. اجرا بدون نصب.
- **Kubuntu:** پوشه‌ی `vault-linux/` مشابه؛ با وابستگی‌های سیستم نصب‌شده یا فریز.
- دو بیلد جدا برای دو سیستم عامل.
- فایل‌های بالای ۱۰MB پلین می‌مانند (پس portable بدون مشکل رمز برای‌شان).

---

## ۱۶. پروژه و ساختار دایرکتوری (پیشنهادی)

```
secure-vault/
├── DESIGN.md                  (این فایل)
├── LICENSE                    (GPL-3.0)
├── README.md
├── pyproject.toml / requirements.txt
├── src/
│   ├── main.py                (اجرای Qt app)
│   ├── core/
│   │   ├── crypto.py          (AES-GCM, HKDF, Argon2id)
│   │   ├── vaultfs.py         (ذخیره/بازیابی فایل، رمز/پلین)
│   │   ├── index.py           (اسکی‌لایت: FTS5, metadata, logs)
│   │   ├── search.py          (لفظی + معنایی opt-in)
│   │   ├── semantics.py       (embedding محلی، اختیاری)
│   │   └── security.py        (سطوح حساسیت، قفل یک‌طرفه)
│   ├── api/
│   │   ├── mcp_server.py      (MCP برای ایجنت)
│   │   └── local_api.py       (API محلی برای UI)
│   ├── ui/
│   │   ├── main_window.py     (Qt main window)
│   │   ├── markdown_view.py   (QtWebEngine markdown editor)
│   │   ├── secret_viewer.py   (پنجره‌ی OS برای فایل رمز)
│   │   ├── log_panel.py
│   │   └── tray.py
│   └── importers/
│       ├── import_joplin.py
│       └── (هر importer جدا)
├── portable/
│   ├── win/                   (بیلد Windows: python embedded + venv)
│   └── linux/                 (بیلد Kubuntu)
```

---

## ۱۷. نکات امنیتی بحرانی (قبل از پیاده‌سازی حتماً بخوان)

1. **کریپتو:** از `cryptography` بسته یا `PyCryptodome` استفاده شود. AES-256-GCM،
   HKDF-SHA256، Argon2id. هرگز key روی دیسک.
2. **حالت locked:** فقط متادیتا در دسترس ایجنت. محتوا هرگز.
3. **محرمانه vs رمز:** محرمانه = درخواست از MCP → تأیید کاربر + notification.
   رمز = فقط درخواست نمایش native، خود کاربر می‌بیند و کپی می‌کند.
4. **قفل یک‌طرفه:** علامت‌گذاری به‌سمت بالا از MCP مجاز، برگشت فقط از UI.
5. **جستجوی معنایی:** opt-in، محلی، هیچ ارسال به بیرون. ایندکس معنایی محافظت شود.
6. **لاگ:** کامل، فقط‌الحاقی، در UI.
7. **Master password:** هرگز در MCP/log/پایپ. فقط از UI keyboard.
